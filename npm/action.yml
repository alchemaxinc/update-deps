name: 'Update NPM Dependencies'

description: 'Update NPM dependencies using npm-check-updates'

inputs:
  token:
    required: false
    description: 'GitHub token'
    default: ${{ github.token }}

  base-branch:
    required: true
    description: 'Base branch for PR'
    default: 'main'

  branch-prefix:
    required: false
    description: 'Branch prefix'
    default: 'update-dependencies'

  pr-title:
    required: false
    description: 'Title for the pull request'
    default: 'Update NPM Dependencies'

  commit-message:
    required: false
    description: 'The commit message for the update'
    default: 'Update NPM dependencies'

  excluded-packages:
    required: false
    description: 'Packages to exclude from updates, in a comma-separated list format'

  relock:
    required: false
    description: 'Whether package-lock.json should be regenerated, even if no other updates are available'
    default: 'false'

  auto-merge:
    required: false
    description: 'Whether a successful PR should be automatically merged'
    default: 'false'

  dry-run:
    required: false
    description: 'Run without creating a PR (for testing purposes)'
    default: 'false'

runs:
  using: 'composite'
  steps:
    - name: Checkout and setup
      if: inputs.dry-run != 'true'
      uses: alchemaxinc/composite-toolbox/checkout-and-setup@v1
      with:
        token: ${{ inputs.token }}

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        cache: 'npm'
        node-version-file: '.nvmrc'

    - name: Cache npm-check-updates
      id: cache-ncu
      uses: actions/cache@v4
      with:
        path: ~/.npm-global
        key: npm-ncu-${{ runner.os }}-${{ hashFiles('**/package-lock.json') }}
        restore-keys: |
          npm-ncu-${{ runner.os }}-

    - name: Install npm-check-updates
      if: steps.cache-ncu.outputs.cache-hit != 'true'
      shell: bash
      run: |
        npm config set prefix '~/.npm-global'
        export PATH=~/.npm-global/bin:$PATH
        npm install -g npm-check-updates

    - name: Update dependencies
      id: update_deps
      shell: bash
      run: |
        export PATH=~/.npm-global/bin:$PATH

        # Capture updates from ncu dry run (lines like "package  1.0.0  →  2.0.0")
        ncu_output=$(ncu --reject "${{ inputs.excluded-packages }}" 2>/dev/null || true)

        # Check if there are any updates available
        update_lines=$(echo "$ncu_output" | grep -E '^\s*\S+\s+\S+\s+→\s+\S+' || true)
        if [ -z "$update_lines" ]; then
          echo "::notice::No updates available, stopping"
          exit 0
        fi

        # Apply updates to package.json
        ncu -u --reject "${{ inputs.excluded-packages }}"

        # Initialize categorized update lists
        major_updates=""
        minor_updates=""
        patch_updates=""

        # Parse and categorize each updated package
        while IFS= read -r line; do
          [ -z "$line" ] && continue
          pkg=$(echo "$line" | awk '{print $1}')
          old_ver=$(echo "$line" | awk '{print $2}')
          new_ver=$(echo "$line" | awk '{print $4}')
          if [ -n "$pkg" ] && [ -n "$old_ver" ] && [ -n "$new_ver" ]; then
            echo "::notice::Updated $pkg from $old_ver to $new_ver"

            # Strip semver prefixes (^, ~, >=, etc.)
            clean_old=$(echo "$old_ver" | sed 's/^[^0-9]*//')
            clean_new=$(echo "$new_ver" | sed 's/^[^0-9]*//')

            # Extract major and minor version numbers
            old_major=$(echo "$clean_old" | cut -d. -f1)
            new_major=$(echo "$clean_new" | cut -d. -f1)
            old_minor=$(echo "$clean_old" | cut -d. -f2)
            new_minor=$(echo "$clean_new" | cut -d. -f2)

            entry="| \`$pkg\` | \`$old_ver\` | \`$new_ver\` |"

            if [ "$new_major" -gt "$old_major" ] 2>/dev/null; then
              major_updates+="${entry}"$'\n'
            elif [ "$new_minor" -gt "$old_minor" ] 2>/dev/null; then
              minor_updates+="${entry}"$'\n'
            else
              patch_updates+="${entry}"$'\n'
            fi
          fi
        done <<< "$update_lines"

        # Build the dynamic PR body
        pr_body_file=$(mktemp)

        echo "# :arrows_counterclockwise: NPM Dependencies Update" >> "$pr_body_file"
        echo "" >> "$pr_body_file"

        if [ -n "$major_updates" ]; then
          {
            echo "## :boom: Major Updates (Breaking Changes)"
            echo ""
            echo "> :warning: **These updates may contain breaking changes. Please review carefully!**"
            echo ""
            echo "| Package | Old | New |"
            echo "| --- | --- | --- |"
            printf "%s" "$major_updates"
            echo ""
          } >> "$pr_body_file"
        fi

        if [ -n "$minor_updates" ]; then
          {
            echo "## :sparkles: Minor Updates"
            echo ""
            echo "| Package | Old | New |"
            echo "| --- | --- | --- |"
            printf "%s" "$minor_updates"
            echo ""
          } >> "$pr_body_file"
        fi

        if [ -n "$patch_updates" ]; then
          {
            echo "## :wrench: Patch Updates"
            echo ""
            echo "| Package | Old | New |"
            echo "| --- | --- | --- |"
            printf "%s" "$patch_updates"
            echo ""
          } >> "$pr_body_file"
        fi

        {
          echo "---"
          echo ""
          echo ":robot: *This PR was automatically generated by the NPM Dependencies Update action*"
        } >> "$pr_body_file"

        # Set the PR body as a step output
        {
          echo "pr_body<<ENDOFBODY"
          cat "$pr_body_file"
          echo "ENDOFBODY"
        } >> "$GITHUB_OUTPUT"

        rm -f "$pr_body_file"

        if [ "${{ inputs.relock }}" = "true" ]; then
          rm -f package-lock.json
        fi
        npm install

    - name: Check for changes
      id: check_changes
      if: inputs.dry-run != 'true'
      uses: alchemaxinc/composite-toolbox/check-changes@v1
      with:
        files: 'package.json package-lock.json'

    - name: Create Pull Request
      id: open-pr
      if: inputs.dry-run != 'true' && steps.check_changes.outputs.has_changes == 'true'
      uses: alchemaxinc/composite-toolbox/create-pr@v1
      with:
        token: ${{ inputs.token }}
        base-branch: ${{ inputs.base-branch }}
        branch-prefix: ${{ inputs.branch-prefix }}
        files: 'package.json package-lock.json'
        commit-message: ${{ inputs.commit-message }}
        pr-title: ${{ inputs.pr-title }}
        pr-body: ${{ steps.update_deps.outputs.pr_body }}

    - name: Auto-merge Pull Request
      if: inputs.dry-run != 'true' && inputs.auto-merge == 'true' && steps.open-pr.outcome == 'success'
      uses: alchemaxinc/composite-toolbox/merge-pr@v1
      with:
        token: ${{ inputs.token }}
        merge-method: 'squash'
        pull-request-number: ${{ steps.open-pr.outputs.pr_number }}
